---
apiVersion: v1
kind: Service
metadata:
  name: support-collect
  namespace: "support-collect"
  labels:
    app: support-collect
spec:
  clusterIP: None
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: support-collect
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
